{
  "name": "harve-landing",
  "private": true,
  "scripts": {
    "build:demo": "esbuild src/demo-entry.jsx --bundle --outfile=demo-bundle.js --format=iife --loader:.jsx=jsx --jsx=automatic --minify"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
